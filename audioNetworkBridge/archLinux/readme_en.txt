First：

The account is the root account

Run 
pacman -S libasound2-dev


Method 1
1. SSH transfers audioNetworkBridge all files to any directory in the System.
2. Run 
   chmod a+x./install.sh 
   on the terminal
3. Run 
   ./install.sh 
   on the terminal

===========================================================

The account no the root account

Run on the terminal
sudo pacman -S libasound2-dev


Method 1
1. SSH transfers audioNetworkBridge all files to any directory in the System.
2. Run 
   sudo chmod a+x./install.sh 
   on the terminal
3. Run 
   sudo ./install.sh 
   on the terminal
4. Run
   sudo ./reboot



